# Avatar Plugin

Adds avatar capability handshake plus an `avatar_express` tool so agents can emit non-verbal cues.

Gateway methods:

- `avatar.hello`
- `avatar.status`
- `avatar.pull`
- `avatar.goodbye`

Agent tool:

- `avatar_express`
