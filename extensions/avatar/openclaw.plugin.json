{
  "id": "avatar",
  "name": "Avatar",
  "description": "Avatar handshake and expression tool for OpenClaw agents",
  "skills": ["./skills"],
  "uiHints": {
    "enabled": {
      "label": "Enabled",
      "help": "Toggle avatar plugin runtime."
    },
    "queueLimit": {
      "label": "Queue Limit",
      "help": "Max queued avatar events per session.",
      "advanced": true
    },
    "wsPort": {
      "label": "Local WS Port",
      "help": "Local extension websocket port (127.0.0.1 only).",
      "advanced": true
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": { "type": "boolean" },
      "queueLimit": { "type": "integer", "minimum": 16, "maximum": 2048 },
      "wsPort": { "type": "integer", "minimum": 1, "maximum": 65535 }
    }
  }
}
